---
# Main sudo tasks

- name: install sudo
  include: install.yml
  tags:
    - role::sudo
    - role::sudo::install

- name: install sudo
  include: install.yml
  tags:
    - role::sudo
    - role::sudo::configure
